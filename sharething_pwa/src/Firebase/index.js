import FirebaseContext, {withFirebase, withFirebaseProvider} from './context';
import Firebase from './Firebase';

export default Firebase;

export {FirebaseContext, withFirebase, withFirebaseProvider};

